# Placeholder — unused until a real AWS account exists (Phase 5 stretch).
# Do NOT put real credentials here; use environment variables or a CI
# secret store for aws_access_key / aws_secret_key instead.
use_localstack = false
aws_region     = "us-east-1"
project_name   = "vibe"
environment    = "prod"
